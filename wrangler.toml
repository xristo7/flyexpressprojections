name = "flyexpressprojections"
compatibility_date = "2026-09-08"
pages_build_output_dir = "./dist"

# Cloudflare Pages (Git):
#   Build command: npm run build
#   Build output:  dist
# SPA routing: public/_redirects (/* /index.html 200) plus dist/404.html copy of index.html
